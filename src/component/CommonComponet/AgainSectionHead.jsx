import React from 'react'

const AgainSectionHead = ({className,children}) => {
  return (
    <div className={`commonSectionHead ${className}`}>{children}</div>
  )
}

export default AgainSectionHead
